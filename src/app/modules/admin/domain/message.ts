export interface OverviewPageMessages {
  deleteConfirmationTitle: {
    single: string;
    multiple: string;
  };
  deleteConfirmationMessage: {
    single: string;
    multiple: string;
  };
  deleteNotification: {
    single: string;
    multiple: string;
  };
  saveNotification: string;
  createNotification: string;
  itemLabel: string;
}
